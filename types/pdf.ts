export type PDFInfoType = {
  numeroFactura: number,
  cliente: string,
  direccion: string,
  fecha: string,
  monto: number,
  montoTexto: string,
  formaPago: string,
  detalles: string,
  codigoVenta: string
}